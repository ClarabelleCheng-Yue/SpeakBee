result-view {
  match:AudioUrl (this) {
    min (Required) max (One)
  }

  render {
    nothing
  }
  
  app-launch {
    requires {
      app-id (“org.tizen.browser”)
    }
    payload-uri {
      template ("browser://www.google.com") //settings/some/path?param=#{value(this)}")
    }
  }
}