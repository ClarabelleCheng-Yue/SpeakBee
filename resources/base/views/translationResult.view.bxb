result-view {
   match:Translation (this)

  // app-launch here
  
   render {
     layout {
       section {
         content {
           single-line {
             text {
               style (Detail_L)
               value ("#{value(this.text)}")
             }
           }
         }
       }
     }
  }
}