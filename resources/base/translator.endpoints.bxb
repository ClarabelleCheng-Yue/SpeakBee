endpoints {
  action-endpoints {
    action-endpoint (Translate) {
      accepted-inputs ($vivContext)
      local-endpoint (translate.js)
    }
    
    action-endpoint (GetPattern) {
      accepted-inputs (fromLang, toLang, $vivContext)
      local-endpoint (getPattern.js)
    }
  }
}
