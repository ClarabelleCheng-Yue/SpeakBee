structure (Translation) {
  description (the final translation)
  property (text) {
    type (Text)
    min (Required) max (One)
  }
  property (audioUrl) {
    type (AudioUrl)
    min (Optional) max (One)
  }
}
