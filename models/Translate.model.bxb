action (Translate) {
  type (Search)
  description (translate from one language to another)
  // collect {
    // input (fromTo) {
    //   type (FromTo)
    //   min (Required) max (One)
    // }
  // }
  output (Translation)
}
